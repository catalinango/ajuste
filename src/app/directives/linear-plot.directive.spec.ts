import { LinearPlotDirective } from './linear-plot.directive';

describe('LinearPlotDirective', () => {
  it('should create an instance', () => {
    const directive = new LinearPlotDirective();
    expect(directive).toBeTruthy();
  });
});
