import { ScatterPlotDirective } from './scatter-plot.directive';

describe('ScatterPlotDirective', () => {
  it('should create an instance', () => {
    const directive = new ScatterPlotDirective();
    expect(directive).toBeTruthy();
  });
});
